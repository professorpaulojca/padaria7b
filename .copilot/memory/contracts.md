# Contracts

## API (endpoints/DTOs)
- A definir na Fase 2/3 (SPEC/PLAN). Nenhum endpoint existe ainda.
- Padrão obrigatório: contratos definidos antes da implementação.

## Padrão de erro
```json
{
  "code": "string",
  "message": "string",
  "details": "string (opcional)"
}
```

## Integrações externas
- iFood: integração conceitual (escopo simplificado). Sem API real definida.
